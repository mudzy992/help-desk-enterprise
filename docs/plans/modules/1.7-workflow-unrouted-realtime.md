# Paket 1.7 — Workflow prikaz, nerutirani tiketi i realtime admin događaji

> Status: **implementirano** (odobreno: SLA teče u ciljnoj grupi, jednokratno upozorenje + crveni brojač + sedmični pregled ponedjeljkom, „Tok statusa" samo ADMIN/SUPER_ADMIN, realtime za sva četiri domena) · Procjena: ~2,5 RD (plan: 2 RD; proširenje zbog nadzora nerutiranih i generičkog admin događaja) · Izvor: gap G8, G9, G10 · RAW §257–260 (state machine guards), §554–557 (unrouted queue), §647–648 (`private.workflow.stateMachine.*`), §749 (`routing.rules.updated`), §882 i §910 (operativno pravilo za nerutirane)

## 1. Cilj

1. Administrator **vidi** kako tiket putuje kroz statuse: koji su prelazi dozvoljeni, ko ih smije napraviti i koji uslovi važe. Bez čitanja koda i bez rizika da neko pokvari tok iz UI-ja.
2. Nijedan tiket ne ostaje zaboravljen u redu nerutiranih: postoji odgovorni (owner), rok za obradu, upozorenje kad rok istekne i opcionalno grupa koja takve tikete prima odmah.
3. Kad jedan admin promijeni pravila rutiranja (ili drugu ključnu konfiguraciju), ekrani drugih admina se osvježe sami.

## 2. Zatečeno stanje (analiza koda)

| Oblast | Stanje |
| --- | --- |
| State machine | `tickets.constants.ts` → `allowedTicketStatusTransitions` (9 statusa). Guardovi su raspoređeni po kodu: `assert-patch-ticket-status.ts` (permisija `canChangeTicketStatus`, zabrana ručnog ulaska u i izlaska iz PENDING_APPROVAL, reopen samo kroz reopen tok), close codes i required fields na RESOLVED/CLOSED, approvals (`decide-ticket-approval.ts`), claim i auto-assign, waiting-for-user automatika, reopen prozor, arhiviranje (worker). |
| Prikaz toka | Ne postoji. Pravila znaju samo developeri. |
| `private.workflow.stateMachine.*` | Ne postoji. Tok je svjesno tvrdo kodiran (G8). |
| Unrouted queue | Tiket bez pravila dobija status `UNROUTED`, `assignedGroupId = null` (`apply-create-ticket-routing.ts`). Postavke `unroutedQueue.enabled` i `ownerRole` postoje, ali su **samo opisne**: `ownerRole` se nigdje ne koristi za obavještavanje, a `enabled=false` ne mijenja ponašanje. |
| `targetGroupId`, `cleanupSlaHours` | Ne postoje. |
| Vidljivost nerutiranih | SUPER_ADMIN vidi sve; ADMIN/AGENT po OU/servis scope-u. Dashboard pokazuje broj UNROUTED, bez starosti. Obrada ide kroz „Proslijedi" (1.1) ili zatvaranje (spam). |
| Realtime | Socket.IO sa Redis adapterom (više API instanci). Sobe: `user:*`, `group:*`, `ticket:*`. `settings.updated` se emituje svima. **`routing.rules.updated` se ne emituje** (G10); drugi admin mora ručno osvježiti stranicu. |

## 3. Odluke dizajna

### W — Workflow: jedan izvor istine i read-only prikaz

**W1 — Definicija toka kao podatak.** Novi modul `tickets/workflow/ticket-workflow-definition.ts` opisuje svaki prelaz:

```ts
{ from: 'IN_PROGRESS', to: 'RESOLVED',
  actors: ['STAFF'],                    // STAFF | REQUESTER | APPROVER | SYSTEM
  trigger: 'status_change',             // status_change | claim | forward | approval | reopen | automation | merge
  guards: ['close_code', 'required_fields'] }
```

`allowedTicketStatusTransitions` se **izvodi** iz ove definicije, pa postojeći guardovi rade identično kao danas. Test garantuje da je izvedena matrica jednaka današnjoj (nula promjena ponašanja). Automatski prelazi, koje danas rade servisi mimo PATCH-a, dobijaju zapis sa `actors: ['SYSTEM']`:
- waiting-for-user → auto zatvaranje;
- nastavak na odgovor korisnika;
- arhiviranje;
- auto-assign;
- propagacija merge statusa.

Ovi zapisi služe samo za prikaz, a assert ih dodatno provjerava.

**W2 — API.** `GET /workflow/ticket-status` (ADMIN i SUPER_ADMIN) vraća:
- statuse;
- prelaze;
- ljudski čitljive guardove;
- **trenutne vrijednosti postavki koje guardove parametrizuju**: close codes uključeni ili obavezni, required fields, reopen prozor (dani), waiting-for-user rok i akcija, approvals uključeni, arhiviranje nakon N dana, UNROUTED owner, rok i ciljna grupa.

Admin tako vidi stvarno ponašanje svoje instalacije, a ne generičku sliku.

**W3 — Ekran „Tok statusa"** (Admin → Tok statusa):
- **Dijagram** (SVG, bez nove biblioteke). Statusi su raspoređeni po fazama (Prijem → Obrada → Završetak), strelice su obojene po akteru: osoblje, podnosilac, odobravač, sistem. Klik na strelicu ili status ističe povezane prelaze.
- **Tabela prelaza:** Od → Do, Ko, Okidač, Uslovi (npr. „Obavezan kod zatvaranja", „Ponovno otvaranje u roku 7 dana"). Tabela je filtrabilna po statusu.
- **Brojač tiketa po statusu** na svakom čvoru. Brojevi dolaze iz postojećeg counts endpointa, u scope-u admina.
- **Info traka:** „Tok je fiksan radi konzistentnosti SLA, odobrenja i izvještaja. Promjene idu kroz razvoj." Uz traku je link na dokumentaciju.
- Dostupno i na engleskom; tekstovi za guardove su i18n ključevi.

**W4 — Uređivanje prelaza iz UI-ja se NE uvodi** (svjesno odstupanje od RAW §648). Razlozi:
- SLA pauze, approvals, reopen, CSAT, izvještaji i time tracking (1.3, T6) oslanjaju se na tačno određene prelaze. Slobodno uređivanje otvara stanja iz kojih nema izlaza ili tikete koji zaobilaze obavezna polja.
- `stateMachine.enabled=false` (isključenje guardova) se ne nudi.

Ovo se dokumentuje u gap izvještaju kao „zatvoreno odlukom".

### U — Nerutirani tiketi

**U1 — Ciljna grupa (`private.ticket.unroutedQueue.targetGroupId`, default prazno).**
- Ako je postavljena i grupa postoji i aktivna je, tiket bez pravila dobija status `PENDING` i `assignedGroupId = target`. Tako ulazi u normalan tok, gdje važe inbox grupe, SLA i auto-assign.
- Tiket dobija oznaku `Ticket.routedByUnroutedFallback = true` (nova kolona) i sistemski događaj u historiji: „Nema pravila rutiranja za OJ X + servis Y — upućeno u grupu Z".
- Pokrivenost rutiranja i dalje prijavljuje rupu, jer ciljna grupa nije pravilo. Admin vidi i broj tiketa koji su tako upućeni.
- Upis postavke validira grupu. Ako grupa kasnije bude obrisana ili deaktivirana, tiket pada nazad u `UNROUTED` (nikad se ne gubi), uz upozorenje u logu i na health/status čipu.

**U2 — Rok obrade (`private.ticket.unroutedQueue.cleanupSlaHours`, default 8; 0 = isključeno).**
- Worker job svakih 15 minuta, u slobodnom slotu između postojećih sweepova, traži:
  - tikete u `UNROUTED` starije od roka;
  - tikete upućene u ciljnu grupu (U1) koji su nakon roka još nedodijeljeni u toj grupi.
- **Jednokratno upozorenje po tiketu.** Nova kolona `unroutedWarnedAt` sprječava ponavljanje, a prosljeđivanje ili dodjela je resetuje.
- **Sedmični pregled (dopuna nakon odobrenja).** Ponedjeljkom u 08:00 (Europe/Sarajevo, uključujući zimsko vrijeme) isti primaoci dobijaju jednu notifikaciju `ticket.unroutedDigest` s brojem tiketa koji su *još uvijek* preko roka, uključujući već upozorene. Klik vodi na listu `?unroutedOverdue=true`. Prekidač: `private.ticket.unroutedQueue.weeklyDigest` (default `true`). Pregled se ne šalje kad nema takvih tiketa ili kad je `cleanupSlaHours = 0`.
- Primaoci notifikacije `ticket.unroutedOverdue` (in-app + e-mail po postavkama 1.5):
  - korisnici s `ownerRole` (default SUPER_ADMIN);
  - članovi ciljne grupe, ako je postavljena.

**U3 — Vidljivost.**
- Dashboard kartica „Nerutirani" pokazuje ukupan broj i broj preko roka („3 · 1 preko 8 h", crveno kad je > 0).
- Lista tiketa dobija brzi filter „Nerutirani preko roka" (parametar `unroutedOverdue=true`). Nerutirani redovi imaju oznaku starosti („stoji 11 h").
- Na detalju nerutiranog tiketa admin s pravom `routing.write` vidi dugme **„Kreiraj pravilo za ovu kombinaciju"**. Dugme otvara Rutiranje sa unaprijed popunjenim OJ i servisom, pa se uzrok rješava, a ne samo simptom.

**U4 — Značenje postojećih postavki (pojašnjenje, bez lomljenja).**
- `ownerRole` sada stvarno određuje primaoce upozorenja iz U2. Vidljivost ostaje po scope-u.
- `enabled=false` znači „ne koristi eksplicitni red". Ako je ciljna grupa postavljena, tiketi idu u nju. Ako nije, tiket ipak ostaje `UNROUTED`, jer je pravilo „bez gubitka tiketa" (RAW §910) jače od prekidača. Opis postavke se ažurira da to kaže.

### R — Realtime admin događaji

**R1 — Admin soba.** Pri konekciji korisnik s ulogom ADMIN ili SUPER_ADMIN ulazi u sobu `role:admins`. Redis adapter već postoji, pa emit radi preko svih API instanci.

**R2 — Događaji.**
- `routing.rules.updated` (naziv iz RAW §749) šalje se u `role:admins` pri kreiranju, izmjeni ili brisanju pravila i pri aktivaciji ili rollbacku config verzije koja mijenja rutiranje.
  - Payload: `{ action, ruleId?, actorUserId, occurredAt }`, bez osjetljivih podataka.
- Generički **`admin.config.updated { domain }`** za ostale ključne konfiguracije koje drugi admini gledaju istovremeno. Domeni:
  - `sla` (politike, kalendari, matrica prioriteta);
  - `catalog` (servisi, forme, dostupnost);
  - `groups` (grupe i članstvo);
  - `routing` (dupli emit radi jednostavnog klijenta).
- Postojeći `settings.updated` ostaje kako jeste.

**R3 — Frontend.**
- `invalidate-on-event` mapira domen na query ključeve, pa se ekrani koji su otvoreni tiho osvježe.
- **Zaštita unosa:** ako admin upravo uređuje pravilo ili formu, ekran se ne osvježava ispod njega. Umjesto toga prikazuje traku „Pravila je upravo izmijenio Emir H. — [Osvježi]".
- Ekran koji nije vidljiv ne povlači ništa (isti princip kao faza 3.3).

## 4. Model podataka (migracija, aditivna)

```
Ticket.routedByUnroutedFallback  Boolean   @default(false)
Ticket.unroutedWarnedAt          DateTime?
@@index([status, createdAt])      -- ako već ne postoji ekvivalent; za U2 sweep
```

Nove postavke: `private.ticket.unroutedQueue.targetGroupId` (string, prazno), `private.ticket.unroutedQueue.cleanupSlaHours` (0–168, default 8) i `private.ticket.unroutedQueue.weeklyDigest` (boolean, default `true`).

## 5. API sažetak

| Metoda | Putanja | Napomena |
| --- | --- | --- |
| GET | `/workflow/ticket-status` | W2, ADMIN/SUPER_ADMIN |
| GET | `/tickets?unroutedOverdue=true` | U3 |
| GET | `/reports/dashboard` (postojeći) | + `unroutedOverdue` brojač |
| WS | `routing.rules.updated`, `admin.config.updated` | R2, soba `role:admins` |

## 6. Testovi

- **Workflow:** izvedena matrica je identična današnjoj; svaki prelaz ima aktera i okidač; nema statusa bez izlaza osim ARCHIVED; API vraća parametre iz postavki.
- **Unrouted:**
  - ciljna grupa hvata tiket bez pravila;
  - neaktivna grupa vraća tiket u UNROUTED;
  - upozorenje ide tačno jednom i tačnim primaocima;
  - prosljeđivanje resetuje upozorenje;
  - `cleanupSlaHours=0` ne šalje ništa;
  - filter liste radi.
- **Realtime:** CRUD pravila emituje `routing.rules.updated` samo u admin sobu; agent ga ne prima; mapiranje domena na query ključeve (vitest).
- **E2E 15:**
  1. admin otvara „Tok statusa" (dijagram i tabela vidljivi);
  2. tiket bez pravila ulazi u ciljnu grupu i ima oznaku;
  3. drugi admin u drugom kontekstu dodaje pravilo, a lista pravila prvog se osvježi bez reloada;
  4. dugme „Kreiraj pravilo" otvara popunjenu formu.

## 7. Van obima

- Uređivanje prelaza iz UI-ja i isključivanje state machine-a (W4).
- Novi statusi (npr. „On hold") pripadaju eventualnom paketu 2.9, uz analizu uticaja na SLA.
- Eskalacija nerutiranih u više nivoa; za sada postoji jedno upozorenje.

## 8. Otvorena pitanja za odobrenje

1. **Ciljna grupa (U1):** tiket upućen u ciljnu grupu dobija status `PENDING` i SLA mu **teče** kao svakom drugom. Alternativa je da SLA miruje dok ga neko ne preusmjeri. *Preporuka: teče,* jer korisniku je svejedno zašto niko ne radi.
2. **Upozorenje (U2):** jednokratno po tiketu ili ponavljano svakih `cleanupSlaHours` dok se ne riješi? *Preporuka: jednokratno + stalni crveni brojač na dashboardu.*
3. **Ekran „Tok statusa" (W3):** samo ADMIN i SUPER_ADMIN, ili i agenti (kao pomoć)? *Preporuka: ADMIN i SUPER_ADMIN.*
4. **Generički admin događaj (R2):** uključiti SLA, katalog i grupe odmah ili samo rutiranje kako RAW traži? *Preporuka: odmah svi četiri domena,* jer je trošak mali, a obrazac je isti.

## 9. Implementacija

### Backend

| Dio | Fajl |
| --- | --- |
| W1 definicija toka kao podatak; `allowedTicketStatusTransitions` se izvodi iz nje | `tickets/workflow/ticket-workflow-definition.ts`, `tickets.constants.ts` |
| W2 `GET /workflow/ticket-status` (ADMIN/SUPER_ADMIN, samo čitanje) | `tickets/workflow/ticket-workflow.controller.ts`, `ticket-workflow.service.ts` |
| Postavke (3 nove) + tolerantni loader | `settings/definitions/ticket-routing-settings.ts`, `tickets/unrouted/unrouted-queue-configuration.loader.ts` |
| U1 fallback u ciljnu grupu (samo ako grupa postoji) | `routing/routing.service.ts` (`resolveUnroutedTargetGroupId`), `routing-configuration.loader.ts`, `tickets/apply-create-ticket-routing.ts`, `create-ticket.ts` |
| U2 reset upozorenja i oznake pri prosljeđivanju | `tickets/forwarding/forward-ticket.ts` |
| U2 sweep (worker, `0 2,17,32,47 * * * *`) + sedmični pregled | `tickets/unrouted/unrouted-sweep.*`, `build-unrouted-overdue-where.ts` |
| U3 filter liste `unroutedOverdue=true` i brojač u `GET /tickets/counts` (`unroutedOverdue`, `unroutedCleanupHours`) | `list/build-ticket-list-filters.ts`, `counts/get-ticket-counts.ts`, `tickets.service.ts` |
| R1 soba `role:admins` | `websocket/websocket.gateway.ts`, `socket-group-membership.service.ts` (`isAdmin`) |
| R2 `admin.config.updated {domain}` + `routing.rules.updated` | `common/admin-realtime/*` (hub, interceptor, dekorator `@AdminConfigDomains`), `websocket/broadcast-user-realtime.ts`, `ticket-chat.gateway.ts` |

Migracija: `prisma/migrations/20261001090000_unrouted_queue_target_cleanup/` (aditivna: `Ticket.routedByUnroutedFallback`, `Ticket.unroutedWarnedAt`). Novi indeks nije potreban: sweep i brojač koriste postojeći `@@index([status, createdAt(sort: Desc)])`.

Dekorator `@AdminConfigDomains` je stavljen na kontrolere rutiranja, grupa, kataloga (servisi, kategorije, forme, dostupnost) i SLA (profili, pravila, kalendari, eskalacije, matrica prioriteta). Na config verzijama emituje samo `activate` i `rollback` (domeni routing, sla, catalog). Događaj ide samo nakon uspješnog odgovora; GET se ignoriše.

### Frontend

- `pages/workflow-page.tsx` (ruta `/admin/workflow`, navigacija „Tok statusa", samo admin), `components/workflow/*`: SVG dijagram po fazama (boja po akteru, isprekidano = sistem, klik ističe prelaze), tabela prelaza s filterom, kartica trenutnih parametara. Raspored i opis uslova su čiste funkcije: `lib/workflow/layout-workflow-diagram.ts`, `describe-workflow-guard.ts`.
- Kartica postavki „Nerutirani": izbor ciljne grupe (s upozorenjem kad grupa ne postoji), rok i sedmični pregled; rok i pregled se uređuju u draweru.
- Dashboard: crvena traka „N nerutiranih čeka duže od X h" → lista s filterom. Lista: checkbox „Nerutirani preko roka" (deep link `?unroutedOverdue=true`).
- Detalj tiketa: oznaka „Bez pravila rutiranja" za tikete iz ciljne grupe i dugme „Kreiraj pravilo za ovu kombinaciju" (uz `routing.write`) → `/routing?originUnitId=…&serviceId=…` otvara tab Pravila s popunjenom formom.
- Realtime: `lib/realtime/admin-config-events.ts` (parsiranje, mapiranje domena na query ključeve, mali bus), `use-admin-config-live-refresh.ts` i `components/admin/admin-config-changed-banner.tsx`. Ožičeno u Rutiranju (pravila), SLA (sve tri vrste prikaza), Grupama i Katalogu servisa.

### Odluke donesene tokom implementacije

1. **Validacija ciljne grupe pri upisu:** `assertValue` postavki je sinhron i nema pristup bazi. Zato UI nudi samo postojeće grupe, a backend na svakom korištenju provjerava postojanje. Obrisana grupa → tiket ostaje `UNROUTED`, sweep loguje upozorenje, kartica postavki i „Tok statusa" prikazuju „grupa ne postoji". Grupa nema `isActive`, pa je „neaktivna" = „ne postoji".
2. **„Admin uređuje"** znači: fokus u polju forme unutar ekrana ili bilo koji otvoren dijalog/drawer. Tada se ekran ne osvježava, nego se prikazuje traka s imenom autora izmjene. Vlastite izmjene se ignorišu (ekran se već osvježio nakon snimanja). Skriven tab se osvježi jednom pri povratku.
3. **Kanal upozorenja:** in-app notifikacija (idempotentna preko `dedupeKey`). E-mail fan-out iz 1.5 je vezan za događaje tiketa; e-mail za upozorenje i pregled ide uz paket 2.2 (postavke notifikacija).
4. **Brojač na dashboardu** dolazi iz `GET /tickets/counts` (keš 5 s), a ne iz dashboard summary-ja (keš 60 s), da ne mijenja format keša summary-ja.

### Testovi

- Backend: `workflow/ticket-workflow-definition.spec.ts`, `unrouted/unrouted-sweep.service.spec.ts` (jednokratno, primaoci, 0 h, obrisana grupa, pregled i zimsko vrijeme), `apply-create-ticket-routing.unrouted.spec.ts`, `common/admin-realtime/admin-config-realtime.spec.ts`, ažurirani wiring workera/rasporeda, `routing.resolution.spec.ts`, `ticket-list-select.spec.ts`.
- Frontend: `layout-workflow-diagram.spec.ts`, `describe-workflow-guard.spec.ts`, `admin-config-events.spec.ts`, ažuriran `build-navigation-commands.spec.ts`.
- E2E: `e2e/tests/15-workflow-unrouted-realtime.spec.ts`.

### Nakon deploya

- Pokrenuti migraciju `20261001090000_unrouted_queue_target_cleanup`.
- Worker dobija novi red `unrouted-sweep` (bez dodatne konfiguracije).
