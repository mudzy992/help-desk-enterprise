# Zvanični review aplikacije EP·HelpDesk — 2026-09-25

Grana: `arena/01a0d46c-help-desk-enterprise` · Obim: backend (NestJS/Prisma), frontend (React/Vite), CI/ops.

## 0. Sažetak

| | Broj |
|---|---|
| Kritično | 1 — **popravljeno** |
| Visoko | 5 — **popravljeno** |
| Srednje | 11 — otvoreno (preporuke u §3) |
| Nisko / higijena | 10 — otvoreno |

Nakon popravki: backend **1478/1478** testova (377 suite-a), frontend **336/336**, typecheck čist
u oba, kontrast audit 0 padova, i18n bs/en 2195/2195 ključeva bez rupa.

**Kako je rađen review.** Aplikacija ima ~1 490 backend i ~540 frontend fajlova, pa pregled
nije bio „linija po liniju" nego po riziku: autentifikacija, autorizacija, administracija
korisnika, instalacija, prilozi, izvoz, realtime, integritet podataka (brojevi tiketa, statusi),
greške/logovi, zavisnosti, frontend sesija/XSS/i18n/bundle. Performanse su već pokrivene
mjerenjima (`PERF_BUDGETS.md` §5). Nalazi koje sam označio „provjeriti" nisu potvrđeni na
stagingu (sandbox ne dopire do `api.desk.ba101.top`).

Odluke vlasnika tokom reviewa: popraviti kritično/visoko odmah; PENDING/ASSIGNED → CLOSED je
bug; imena priloga normalizovati; login limit 5 pokušaja / 15 min.

---

## 1. Popravljeno u ovom reviewu

| # | Ozbiljnost | Nalaz | Popravka | Commit |
|---|---|---|---|---|
| K1 | **Kritično** | `UsersController` pušta i ADMIN i SUPER_ADMIN. `POST /users/:id/reset-password` vraća privremenu lozinku u odgovoru → **ADMIN je mogao resetovati lozinku SUPER_ADMIN-a i preuzeti instalaciju**; mogao ga je i obrisati/deaktivirati/mijenjati mu e-mail i uloge. | `assertCanManageTargetUser`: samo SUPER_ADMIN upravlja SUPER_ADMIN nalogom (reset, izmjena, brisanje, uloge). Zabrana samodeaktivacije. Nova poruka `users.errorSuperAdminManage` (bs/en). | ba5c91d |
| V1 | Visoko | **Nema zaštite od pogađanja lozinke** na `POST /auth/login`. | `LoginAttemptLimiter`: 5 neuspjeha / 15 min po e-mailu + IP-u, brojači u Redisu (vide ih sve replike), uspješna prijava briše brojač, ispad Redisa ne zaključava nikoga (fail-open). 429 + poruka na login stranici. `TRUST_PROXY=1` da `request.ip` bude klijent, a ne Traefik. | 69b3549 |
| V2 | Visoko | **Utrka pri dodjeli broja tiketa**: `max + 1` bez zaključavanja; dva istovremena kreiranja (različiti podnosioci) → isti broj → P2002 → HTTP 500. | `withTicketNumberRetry`: cijela transakcija se ponavlja (do 5×) samo na koliziju `ticketNumber`. | ba5c91d |
| V3 | Visoko | **Curenje konfiguracije bez prijave**: nakon instalacije `GET /install/super-admin`, `/smtp`, `/login-provider`, `/seed` i dalje javni (e-mail SUPER_ADMIN-a, SMTP host/korisnik, Entra tenant). | Nakon završetka instalacije otvoreni ostaju samo `/install/status` i `/install/addons` (treba ih frontend). | f97bd3d |
| V4 | Visoko (UX) | **Prilozi s razmakom ili č/ć/š/ž/đ odbijani** (`Izvještaj mart.pdf`, `Scan 001.pdf`); multer UTF-8 imena dekodira kao latin1. | Originalno ime se čuva za prikaz; ekstenzija iz ASCII oblika; latin1→UTF-8 dekodiranje; `Content-Disposition` s `filename*` (RFC 5987). Putanje (`/`, `\`, `..`) se i dalje odbijaju. Na disku je ime i dalje `<uuid>.<ext>`. | 9efffab |
| V5 | Visoko (logika) | PENDING/ASSIGNED tiket (duplikat, spam, povučen zahtjev) nije se mogao direktno zatvoriti. | Dodani prelazi PENDING→CLOSED i ASSIGNED→CLOSED (backend + frontend); close code / obavezna polja i dalje važe za CLOSED. | 7860c7d |
| — | Higijena | `sharp` u zavisnostima, nigdje korišten, s poznatim CVE-ovima (libvips). | Uklonjen. | 7860c7d |

Svaka popravka ima test (`assert-can-manage-target-user.spec`, `login-attempt-limiter.spec`,
`generate-ticket-number.spec`, `is-install-wizard-mutation-locked.spec`,
`sanitize-attachment-filename.spec`, `assert-ticket-status-transition.spec`, `ticket-actions.spec`).

**Za deploy:** postaviti `TRUST_PROXY=1` na API servisu u Coolifyju (inače je login limit
efektivno po e-mailu, što je i dalje zaštita, samo grublja).

---

## 2. Backend — nalazi po oblastima

### 2.1 Autentifikacija i sesija
- ✅ Dobro: bcrypt cost 12, dummy hash protiv timing razlike, JWT secret ≥ 32 znaka iz postavki,
  session token odbija „purpose" claim (token za promjenu lozinke ne može biti sesija), guard
  provjerava `isActive` i `mustChangePassword`, SUPER_ADMIN samo lokalni.
- **S1 (srednje) — nema odjave na serveru ni opoziva tokena.** JWT živi 8 h; „Odjava" samo briše
  localStorage. Ukraden token vrijedi do isteka. Preporuka: `sessionVersion` na korisniku (već
  postoji verzionisani principal cache `authz:{id}:v{n}` — isti mehanizam), `POST /auth/logout`
  koji ga podiže, i kraći TTL (npr. 1 h) + refresh.
- **S2 (srednje) — sesije ostaju nakon promjene lozinke.** Isto rješenje kao S1.
- N1 (nisko) — `POST /auth/change-password` bez limita pokušaja (token vrijedi 15 min; nizak rizik).

### 2.2 Autorizacija i administracija
- ✅ Dodjela SUPER_ADMIN uloge već je bila zaštićena (`assertCanAssignRole`); sada i upravljanje (K1).
- **S3 (srednje) — `GET /users` vraća sve korisnike bez paginacije**, a
  `resetUserTemporaryPassword` učitava cijelu listu da nađe jednog. Kod 5–10k korisnika (Entra
  sync) to je spor i velik odgovor. Preporuka: paginacija + `findUnique` za jednog.
- N2 — `RoleGuard` + provjera u servisu su razbacani (dio kontrolera provjerava u servisu, dio
  dekoratorom). Radi, ali otežava review; preporuka: jedna konvencija.

### 2.3 Instalacija
- ✅ Mutacije su zaključane nakon završetka; sada i čitanja (V3).
- **S4 (srednje) — prije završetka instalacije wizard je potpuno javan.** Ko prvi dođe do svježe
  instance postaje SUPER_ADMIN. Preporuka: `INSTALL_TOKEN` iz env-a koji wizard traži.

### 2.4 Tiketi
- ✅ Vidljivost (lista/detalj/brojači) dijeli jedan builder; interne bilješke se filtriraju i u
  listi poruka i u WS (staff/public sobe).
- **S5 (srednje) — `readHighestTicketSequence` radi regex nad svim brojevima** pri svakom
  kreiranju (seq scan; na 100k ~desetine ms). Preporuka: Postgres `SEQUENCE` za brojeve tiketa
  (rješava i V2 bez retryja).
- **S6 (srednje) — poruke tiketa se učitavaju sve, bez paginacije**, pa filtriraju u memoriji.
  Dugi tiketi (stotine poruka) → velik odgovor. Preporuka: kursor paginacija.
- N3 — `UNROUTED` može samo u PENDING (ne može se zatvoriti kao spam bez rutiranja). Provjeriti
  da li je to željeno — isti argument kao V5.
- N4 — `ASSIGNED → RESOLVED` nije dozvoljeno (mora preko IN_PROGRESS). Vjerovatno namjerno.

### 2.5 Prilozi
- ✅ Detekcija potpisa fajla, lista dozvoljenih/opasnih ekstenzija, `<uuid>.<ext>` na disku,
  zaštita od izlaska iz upload root-a.
- **S7 (srednje) — upload ide u memoriju s tvrdim limitom 100 MB** (`memoryStorage`). Nekoliko
  istovremenih velikih uploada = stotine MB RAM-a po instanci. Preporuka: spustiti tvrdi limit
  na konfigurisani max (npr. 25 MB) ili disk storage + stream.
- **S8 (srednje) — `multer` (preko `@nestjs/platform-express`) ima poznat DoS CVE** (crafted
  multipart field names). Preporuka: podići `@nestjs/platform-express` na verziju s popravljenim
  multerom.
- N5 — nema antivirus skeniranja (ClamAV) — nadogradnja za produkciju.

### 2.6 Izvoz
- ✅ CSV neutralizuje formule (`= + - @ \t \r`), UTF-8 BOM za Excel.

### 2.7 HTTP sloj i greške
- ✅ Standardni oblik greške s `requestId`, 500 ne otkriva stack, DB zasićenje → 503.
- **S9 (srednje) — nema sigurnosnih HTTP zaglavlja** (helmet: `X-Content-Type-Options`,
  `X-Frame-Options`/CSP `frame-ancestors`, `Referrer-Policy`, HSTS). Može i na Traefiku;
  preporuka: `helmet` u `main.ts` (bez CSP-a za API) i CSP na frontend nginx-u.
- N6 — `ValidationPipe` je po kontroleru, a ne globalno; novi kontroler bez njega prima
  nevalidiran body. Preporuka: globalni pipe s `whitelist` + `forbidNonWhitelisted`.
- N7 — staging radi s `NODE_ENV=development`.

### 2.8 Zavisnosti (npm audit, runtime)
- Backend: 7 „high" — `multer` (S8), `deepmerge-ts`/`@prisma/config` (build-time),
  `mysql2` (tranzitivno iz `prisma`, ne koristi se — Postgres), `sharp` (uklonjen).
- Frontend: 2 „moderate" — `react-router` open redirect preko `\` u `<Link>`/`navigate`.
  Aplikacija redirektuje samo na `location.pathname` (interni), rizik nizak; preporuka: podići
  `react-router-dom` na zakrpljenu 6.x.

### 2.9 Realtime / Redis / worker
- ✅ WS join provjerava pristup tiketu; staff/public razdvojeni; SLA skener u worker procesu;
  emitovi ~11/s na 200 VU.
- (Poznato, van koda) Redis bez lozinke i izložen na `0.0.0.0:6379` — vlasnik rješava.

---

## 3. Frontend — nalazi

- ✅ Nema `dangerouslySetInnerHTML`/`innerHTML` nigdje → XSS površina minimalna.
  i18n potpun (bs = en), kontrast OK u svim paletama (nakon c503cdf).
- **S10 (srednje) — istek sesije usred rada.** Nema globalnog 401 handlera: nakon 8 h svaki
  ekran pokazuje svoju grešku („nije autorizovano") umjesto preusmjeravanja na prijavu.
  Preporuka: u `apiRequest` na 401 → `clearStoredSession()` + redirect na `/login?from=…`.
- **S11 (srednje) — jedan JS bundle od 1,27 MB (361 KB gzip)**, sve stranice statički uvezene.
  Preporuka: `React.lazy` po ruti (admin, izvještaji, SLA, instalacija) — očekivano ~50 % manji
  početni paket.
- N8 — token u `localStorage` (standardno za SPA; uz nulti XSS i CSP prihvatljivo). Alternativa
  je httpOnly cookie + CSRF — veća promjena.
- N9 — ruta `/_visual-qa` je u produkcijskom bundle-u (iza prijave). Preporuka: samo u dev buildu.
- N10 — bedž broja nepročitanih ima 8,5 px tekst — kontrast sad prolazi, ali veličina je na
  granici čitljivosti.

---

## 4. Ops / repo

- Korijen repoa sadrži stare `*.patch` fajlove (`f1-*.patch`, `f2-*.patch`,
  `fix-dashboard-*.patch`) i `EPHELPDESK.docx/.pdf` — preporuka: premjestiti u `docs/archive/`
  ili obrisati ako su primijenjeni.
- `npm run lint` u backendu ne radi (ESLint flat config) — lint zapravo nije u CI-ju.
- `log_min_duration_statement=100` je uključen na stagingu (namjerno za mjerenje).
- CI akcije su na Node 24 (2819180); kapija DB upita 2.1.

---

## 5. Predložene nadogradnje (redoslijed po vrijednosti)

1. Opoziv sesija + odjava na serveru + kraći TTL s refreshom (S1, S2).
2. Globalni 401 → prijava (S10) — mala izmjena, veliki UX dobitak.
3. `INSTALL_TOKEN` za wizard (S4).
4. Podizanje `@nestjs/platform-express` (multer CVE) i `react-router-dom` (S8, §2.8).
5. helmet + CSP (S9), globalni `ValidationPipe` (N6).
6. Postgres sequence za brojeve tiketa (S5).
7. Paginacija korisnika i poruka (S3, S6); upload limit (S7).
8. Lazy učitavanje ruta (S11).
9. Antivirus za priloge (N5), lint u CI-ju.

---

## 6. Otvorena pitanja za vlasnika

1. **N3:** smije li UNROUTED tiket biti zatvoren direktno (spam prije rutiranja)?
2. **S1/S2:** koji TTL sesije želite za pilot (sada 8 h bez refresha)?
3. **S4:** hoćete li `INSTALL_TOKEN` (utiče na postupak instalacije novog okruženja)?
