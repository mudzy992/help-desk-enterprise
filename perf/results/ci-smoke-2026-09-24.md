# Prvi realni perf smoke — 2026-09-24

Run: [`35986344908`](https://github.com/mudzy992/help-desk-enterprise/actions/runs/35986344908)
(grana `arena/01a0cefc-help-desk-enterprise`, PR #2). Profil: **200 VU / 5 min**,
Postgres + Redis kao service containeri, pragovi iz `perf/config.js::ciThresholds`
(budžet × 2,5). Trajanje joba 6 m 56 s.

Ovo je prvi put da je `perf/smoke.js` stvarno izvršen protiv žive aplikacije —
do sada su svi SLO redovi bili **kod-nalaz**. Zato ovaj dokument sadrži i ono što
je run otkrio, a ne samo brojke.

## 1. Rezultat

| Prag (CI) | Rezultat |
|---|---|
| `http_req_failed: rate<0.01` | ✅ |
| `tickets_list_duration: p(95)<500` | ✅ |
| `ticket_detail_duration: p(95)<500` | ✅ |
| `unread_count_duration: p(95)<500` | ✅ |
| `search_duration: p(95)<500` | ✅ |
| `dashboard_summary_duration: p(95)<500` | ✅ |
| `ticket_message_duration: p(95)<1000` | ✅ |
| `ticket_list_payload_kb: p(95)<100` | ✅ |
| `dashboard_payload_kb: p(95)<100` | ✅ |

Saobraćaj: **27.653 HTTP zahtjeva** u 5 minuta (≈ 92 req/s), 0 neuspjelih odgovora
po ruti (nijedan non-2xx u `api.log`).

## 2. DB upiti po zahtjevu (prvo mjerenje)

`db_queries_per_request` iz `api.log`, bootstrap (`/install/*`, `/health`,
`/auth/login|logout|refresh`) izuzet:

| Metrika | Prije `perf-07` | Poslije `perf-07` |
|---|---|---|
| Prosjek (aplikacijski zahtjevi) | 18,4 preko 27.653 zahtjeva | **3,6** preko 27.792 zahtjeva |
| p95 / max | 29 / 37 | **5 / 13** |
| `GET /tickets/inbox` | 28,6 | **5,0** |
| `GET /tickets` | 25,7 | **5,0** |
| `GET /search` | 13,2 | **6,0** |
| `GET /notifications/unread-count` | 2,0 | **1,0** |

> **Ispravka (isti dan, poslije popunjavanja mjernih praznina).** Ove brojke su mjerene na
> **praznoj** bazi: nijedan tiket nije postojao, pa su batch loaderi (`loadTicketDisplayLabels`,
> `loadParentTicketSummaries`, `loadTicketCsatSubmissions`, `loadCloseCodesByIds`) preskočili
> svoje upite. Sa 120 seedanih tiketa isti kod mjeri **9,6** upita po zahtjevu — vidi
> „Mjerenje s podacima" niže. Prag izmjeren na praznoj aplikaciji ne mjeri ništa.

Latencija istog runa (P95, ms) — ista putanja, samo bez N+1:

| Ruta | Prije | Poslije |
|---|---|---|
| `GET /tickets` | 57,8–2.484 | **11,1** |
| `GET /notifications/unread-count` | 14,5–123 | **4,9** |
| `GET /search` | 248–315 | **80,5** |

**Nalaz.** Red u `PERF_BUDGETS.md` §1 („≤ 2 upita prosječno", kod-nalaz iz F1) opisuje
*podatkovni* upit liste (paginacija + count iz F1.1), ne cijeli zahtjev. Najveći dio
ostatka bili su **settingsi**: svaki `*-configuration.loader.ts` (45 fajlova) čita svoje
ključeve kroz `SettingsService`, jedan `await` po ključu, a svaki `getSetting` je bio
zaseban `appSetting.findUnique`. Tok liste je tako plaćao ~12 od 18 upita.

`perf-07` uvodi snapshot po zahtjevu (`settings-snapshot.ts`): cijela tabela `AppSetting`
(~100 malih redova) se pročita jednom, a svaki sljedeći ključ se odgovara iz memorije.
Snapshot je vezan za `requestId` (nikad se ne dijeli između zahtjeva), van zahtjeva se
ponaša kao prije, a read-after-write u istom zahtjevu je pokriven — vidi `settings-snapshot.spec.ts`.

Ostatak (3,6) su batch loaderi po stranici (`to-ticket-client-responses`,
`load-ticket-display-labels` 5 upita, SLA snapshot, close codes, CSAT) i sesija/authz
(keširano u Redisu). Loaderi su **batchovani** (nema N+1 po redu), pa je dalje smanjenje
pitanje broja rundi, ne arhitekture.

- **Kapija** u CI-ju: `DB_QUERY_BUDGET=8` = izmjereno 3,6 × ~2,2 (pravilo „prag iz
  mjerenja", plan §4.2; širi množitelj od 1,5 da običan jitter ne obara build).
  Regresija kakvu je `perf-07` uklonio (18 > 8) bi i dalje pala.
- **Cilj**: `DB_QUERY_TARGET=3` (radni zadatak, vidi §4).
- `/notifications/unread-count` = 2 upita ✅ — F1.3 (uklonjen polling) se vidi u brojkama.

## 3. Šta je run otkrio (i popravljeno)

Prvi runovi nisu bili mjerenje, nego tri defekta u samom mjernom lancu:

1. **Kapija nije blokirala.** `k6 run … | tee` bez `set -o pipefail` → status je bio
   `tee`-jev (0), pa je `http_req_failed: FAILED` prošao nezapaženo. Sada pipefail stoji,
   a korak budžeta ima `if: always()`.
2. **k6 ugovor o pozivu.** k6 zove `exec` funkciju scenarija **samo** sa `setup()`
   podacima (`fn(data)`), a scenariji su bili izvezeni direktno kao `(config, data)` →
   `TypeError: Cannot read property 'tokens' of undefined` u svakom VU: 10.163.400
   neuspjelih iteracija, gigantski log, job od 35 minuta. Sada postoje jednoparametarski
   wrapperi, a `perf/validate.js` statično provjerava arnost (sekcija „k6 scenario
   contract") da se to ne vrati.
3. **Zastarjeli kontrakt u harnessu.** `searchHeavy` je zvao `/tickets?search=`, a F1.1 je
   parametar preimenovao u `q` → 20 × `400`, `http_req_failed` preko 1 %. Aplikacija je
   bila u pravu; harness nije. Sada `/tickets?q=`.

Uz to: nepotpun install wizard u CI-ju (`/install/login-provider` i `/install/complete`
nisu zvani) → `POST /auth/login` = **503**, pa je smoke mjerio prazno; i `awk` je dijelio
jednu rutu na dva ključa zbog ANSI boja u Nest logu (svaki broj se duplo dijelio).

## 3b. Mjerenje s podacima (run 35989311688)

Prvi run u kojem je baza imala tikete (120 kroz `POST /tickets`) i u kojem su pozvani
`GET /tickets/:id`, `POST /tickets/:id/messages` i `GET /reports/dashboard/summary`.
Pragovi: **9/9 ok**, 0 non-2xx.

| Metrika | Prazna baza | S podacima |
|---|---|---|
| avg `db_queries_per_request` | 3,6 | **9,6** (72.917 zahtjeva) |
| p95 / max | 5 / 13 | 14 / 29 |
| `GET /tickets` | 5,0 | 12,1 |
| `GET /tickets/inbox` | 5,0 | 12,0 |
| `GET /tickets/:id` | 0,00 (nije mjereno) | 14,0 |
| `POST /tickets/:id/messages` | 0,00 (nije mjereno) | 9,9 |
| `GET /reports/*/summary` | 0,00 (nije mjereno) | 1,1 |
| `GET /search` | 6,0 | 6,0 |
| `GET /notifications/unread-count` | 1,0 | 1,0 |

| Ruta | P95 (ms) | Max (ms) |
|---|---|---|
| `GET /tickets` | 20,31 | 2.033 |
| `GET /tickets/:id` | 19,42 | 383 |
| `POST /tickets/:id/messages` | 184,59 | 386 |
| `GET /search` | 118,58 | 197 |
| `GET /notifications/unread-count` | 7,31 | 180 |
| `GET /reports/*/summary` | 6,11 | 244 |

Payload: lista **43,84 KB** (budžet 100 KB), dashboard **0,35 KB**.

**Dva defekta mjernog lanca koja je ovaj run otkrio** (oba bi tek sada postala vidljiva):

1. `POST /tickets/:id/messages` je slao `{ body, visibility }`, a `CreateTicketMessageDto`
   je tačno `{ type, body }` i kontroler radi s `forbidNonWhitelisted: true` — prvi run
   koji stvarno stigne do te rute dobio bi 400. Popravljeno na `{ type: 'AGENT_REPLY', body }`.
2. Tiket kreiran bez routing pravila izlazi kao `UNROUTED` bez grupe, a grupni inbox lista
   samo neasignirane `PENDING` tikete **s grupom** — zato seed korak čita OU i servis iz
   `/install/seed` (koji pravi i routing pravilo), a ne iz `GET /services`.

**Kapija.** 8 je bilo izvedeno iz 3,6 — brojke s prazne baze. Sada: `DB_QUERY_BUDGET=12`
(izmjereno 9,6 × ~1,25), `DB_QUERY_TARGET=8` ostaje kao inženjerski cilj.

**Gdje odlazi 12 upita po listi** (recon koda): paginacija 2 (`findMany` + `count`),
`loadTicketVisibilityInputs` 2 (sve OU + `groupMember`), `authorization-context.loader`
(sesija/authz), settings snapshot 1, display labels 5 (user, grupa, form version, OU,
servis), SLA snapshot 1, CSAT 1. Vidljivi duplikati: OU se u istom zahtjevu čita dvaput
(`loadTicketVisibilityInputs` sve OU, `loadTicketDisplayLabels` po ID-u), a `groupMember`
i autorizacijski kontekst čitaju iste redove.

## 3c. Zeleni run s kapijom 12 (run 35990092260)

Prvi run koji je prošao **i** sa svim metrikama popunjenima: pragovi 9/9, 0 non-2xx,
`DB_QUERY_BUDGET=12` ✓.

| Metrika | Vrijednost |
|---|---|
| avg `db_queries_per_request` | **8,742** preko 54.319 zahtjeva (kapija 12) |
| p95 / max | 14 / 29 |
| `GET /tickets` | 12,15 (9.850 zahtjeva) |
| `GET /tickets/inbox` | 12,01 (8.261) |
| `GET /tickets/:id` | 13,99 (16.388) |
| `POST /tickets/:id/messages` | 9,79 (700) |
| `GET /reports/*/summary` | 1,03 (9.510) |
| `GET /search` | 6,00 (100) |
| `GET /notifications/unread-count` | 1,00 (9.510) |

| Ruta | P95 (ms) | Max (ms) |
|---|---|---|
| `GET /tickets` | 33,08 | 2.645 |
| `GET /tickets/:id` | 31,91 | 452 |
| `POST /tickets/:id/messages` | 268,28 | 409 |
| `GET /search` | 105,10 | 355 |
| `GET /notifications/unread-count` | 12,52 | 157 |
| `GET /reports/*/summary` | 10,31 | 343 |

Seed: 120 tiketa, 0 neuspjelih, 5 vidljivih u `/tickets/inbox` (dokaz da routing
dodjeljuje grupu). Payload lista 43,84 KB, dashboard 0,35 KB.

**Cilj ≤ 8 još nije dostignut** (8,74; liste i detalj su 12–14). Preostali kandidati,
po redu rizika: (1) OU se u istom zahtjevu čita dvaput — `loadTicketVisibilityInputs`
učitava *sve* jedinice, `loadTicketDisplayLabels` ih onda čita još jednom po ID-u;
(2) `findMany` + `count` u dva upita gdje `count(*) OVER ()` daje oboje; (3) keš
kataloga (korisnik, grupa, form version, jedinica, servis) po uzoru na
`principal-context.loader` (authz keš s TTL-om, faza 2.2). Sve tri traže
integracionu provjeru na Postgresu — u sandboxu je nema, pa su ostavljene vlasniku.

## 3d. Keš labela — cilj ≤ 8 dostignut (run 35991988771)

Drugi zeleni run s popunjenim metrikama, poslije keša pet kataloga koje lista razrješava
(korisnik, grupa, form version, jedinica, servis) — `labels/ticket-label-cache.ts`, TTL 60 s,
isti obrazac kao keš autorizacijskog konteksta i dashboarda.

| Metrika | Prije keša | Poslije keša |
|---|---|---|
| avg `db_queries_per_request` | 8,742 | **5,005** (46.213 zahtjeva) |
| p95 / max | 14 / 29 | **9 / 29** |
| `GET /tickets` | 12,15 | **7,33** |
| `GET /tickets/inbox` | 12,01 | **7,01** |
| `GET /tickets/:id` | 13,99 | **8,97** |
| `POST /tickets/:id/messages` | 9,79 | 9,82 |
| `GET /reports/*/summary` | 1,03 | 1,02 |
| `GET /search` | 6,00 | 6,00 |
| `GET /notifications/unread-count` | 1,00 | 1,00 |

| Ruta | P95 (ms) | Max (ms) |
|---|---|---|
| `GET /tickets` | 24,44 | 2.337 |
| `GET /tickets/:id` | 24,30 | 573 |
| `POST /tickets/:id/messages` | 279,59 | 349 |
| `GET /search` | 353,05 | 382 |
| `GET /notifications/unread-count` | 9,57 | 288 |
| `GET /reports/*/summary` | 7,65 | 306 |

Pragovi 9/9, 0 non-2xx. **Kapija je pomjerena s 12 na 8** (izmjereno 5,0 × 1,5 — pravilo
„prag iz mjerenja"), a **cilj je s ≤ 8 na ≤ 5**, jer je stari cilj dostignut. Time kapija i
dalje stoji iznad stvarnog stanja (5,0 < 8), pa regresija bilo kojeg od tri koraka pada.

Šta je ostalo (7,3 po listi): paginacija 2 upita (`findMany` + `count`), `loadTicketVisibilityInputs`
2 (sve jedinice + `groupMember`), sesija/authz 1, settings snapshot 1, SLA snapshot 1, CSAT 1 —
plus jedinica koja se u istom zahtjevu čita dvaput (`loadTicketVisibilityInputs` učitava sve,
`loadTicketDisplayLabels` po ID-u; keš to sada pokriva preko zahtjeva, ali ne unutar prvog).

## 4. Sljedeći korak

1. **Staging, puni profil** (`k6 run perf/full.js`, 2.800 VU / 30 min): samo to daje
   izmjerene P95 brojke za `PERF_BUDGETS.md` §1 — `ops/staging-checklist.md` §3.
   CI smoke (200 VU na 2-vCPU runneru) je **regresiona kapija**, ne mjerenje SLO-a.
2. ~~**Popuniti mjerne praznine**~~ — **završeno** (run 35989311688): smoke je mjerio samo `GET /tickets`, inbox, search i
   unread-count. `GET /tickets/:id`, `POST /tickets/:id/messages` i
   `GET /reports/dashboard/summary` su stajali na **0,00** i „prolazili" bez mjerenja —
   u CI-ju nije postojao nijedan tiket, a nijedan scenario nije zvao dashboard.
   Riješeno u `perf-smoke.yml`: korak *Seed load-test tickets* (120 tiketa kroz javni
   API) + dashboard poziv u `browserDashboard` scenariju.
3. ~~**Dalje smanjenje upita** (cilj ≤ 8)~~ — **dostignuto** (5,0; vidi §3d). Novi cilj
   je ≤ 5: jedinica se u prvom zahtjevu čita dvaput (`loadTicketVisibilityInputs` +
   `loadTicketDisplayLabels`), `findMany` + `count` daju dva upita gdje `count(*) OVER ()`
   daje oboje u jednom, a `loadTicketVisibilityInputs` čita *sve* jedinice da bi izračunao
   scope. `load-ticket-display-labels` (5 upita: user,
   grupa, form version, OU, servis) i `close codes` + `CSAT` + SLA snapshot su odvojeni
   batch upiti; spajanje traži sirovi SQL/`$queryRaw` join ili keš tih katalog-a (mijenjaju
   se rijetko, a `perf-07` je pokazao koliko takav keš nosi).
5. **Potvrditi kapiju/cilj** (12/8) u `PERF_BUDGETS.md` — brojke su izmjerene, ali
   politika „koliko je prihvatljivo" je odluka vlasnika.
