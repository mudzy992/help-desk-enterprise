diff --git a/.github/workflows/perf-smoke.yml b/.github/workflows/perf-smoke.yml
index 073707c..6c4b50e 100644
--- a/.github/workflows/perf-smoke.yml
+++ b/.github/workflows/perf-smoke.yml
@@ -17,6 +17,12 @@ concurrency:
   group: perf-smoke-${{ github.ref }}
   cancel-in-progress: true
 
+permissions:
+  contents: read
+  # The run publishes its numbers as a PR comment: artefacts and logs are awkward to
+  # reach outside a browser session, while a comment is readable from the API/CLI.
+  pull-requests: write
+
 jobs:
   perf-smoke:
     name: k6 smoke (CI thresholds)
@@ -54,7 +60,19 @@ jobs:
       CORS_ORIGIN: http://127.0.0.1:5173
       # The query-budget tool (plan §4.2, item 2) is OFF in production and ON here.
       DB_QUERY_METRICS: "true"
-      DB_QUERY_BUDGET: "3"
+      # Measured on seeded data (120 tickets, CI smoke 2026-09-24): 5.0 queries per
+      # application request, down from 18.4 through three steps — the settings
+      # snapshot (18.4 -> 9.6), the gate being put on the real measurement, and the
+      # sixty-second cache of the five display-label catalogues (9.6 -> 5.0). The
+      # gate is the measurement × 1.5, so jitter does not fail the build while a
+      # regression of any of those three does. The target is the engineering goal
+      # tracked in PERF_BUDGETS.md.
+      #
+      # Note the 3.6 this gate briefly claimed: that was the same run on an EMPTY
+      # database. The batch loaders skip their round trips when there is nothing to
+      # resolve, so an empty app looks cheap. Only seeded data measures real queries.
+      DB_QUERY_BUDGET: "8"
+      DB_QUERY_TARGET: "5"
       DB_QUERY_BUDGET_WARN: "true"
       # Smoke profile + relaxed, documented thresholds.
       PERF_PROFILE: ci
@@ -100,26 +118,157 @@ jobs:
           tail -50 "$REPO_ROOT/api.log"
           exit 1
 
-      - name: Seed the minimal fixture (install wizard endpoints)
-        working-directory: backend
+      - name: Run the install wizard (super-admin -> login-provider -> seed -> complete)
+        env:
+          PERF_ADMIN_EMAIL: perf-admin@example.test
+          PERF_ADMIN_PASSWORD: Perf-Smoke-1!
+        run: |
+          # The wizard is a sequence, and the CI fixture used to run only half of it:
+          # without `login-provider` the setting `private.auth.mode` stays unset and every
+          # `POST /auth/login` answers 503 AUTHENTICATION_UNAVAILABLE; without `complete`
+          # there is no JWT signing secret. k6 then reported "2 requests, 0s wall clock".
+          #
+          # Every step is checked, and a failure prints the response body — a silent
+          # failure here cost a day of "the smoke gate is green but empty".
+          rm -f /tmp/install-wizard.log
+          note() { echo "$*" | tee -a /tmp/install-wizard.log; }
+
+          post() {
+            local path="$1" payload="$2" allowed="$3" file status
+            file="/tmp/install$(echo "$path" | tr '/' '-').json"
+            if [ -n "$payload" ]; then
+              status=$(curl -sS -o "$file" -w '%{http_code}' -X POST "$BASE_URL$path" \
+                -H 'content-type: application/json' -d "$payload")
+            else
+              status=$(curl -sS -o "$file" -w '%{http_code}' -X POST "$BASE_URL$path")
+            fi
+            note "$path -> HTTP $status"
+            note "  $(head -c 400 "$file" 2>/dev/null)"
+            # Regex, not `case` with a variable pattern: an expanded `20*|409` is a single
+            # literal pattern in bash (`|` is not alternation after expansion), so a
+            # perfectly good 201 was reported as a failure.
+            if ! printf '%s' "$status" | grep -qE "^($allowed)$"; then
+              note "::error::$path returned HTTP $status"
+              exit 1
+            fi
+          }
+
+          post /install/super-admin \
+            "{\"email\":\"$PERF_ADMIN_EMAIL\",\"displayName\":\"Perf Smoke\",\"password\":\"$PERF_ADMIN_PASSWORD\"}" "20[0-9]|409"
+          post /install/login-provider '{"mode":"local"}' "20[0-9]"
+          post /install/seed "" "20[0-9]"
+          post /install/complete "" "20[0-9]"
+
+          # Prove that the account the k6 scenarios log in with actually works: a 401 here
+          # is the difference between "no load at all" and a real smoke run.
+          login_status=$(curl -sS -o /tmp/login.json -w '%{http_code}' \
+            -X POST "$BASE_URL/auth/login" \
+            -H 'content-type: application/json' \
+            -d "{\"email\":\"$PERF_ADMIN_EMAIL\",\"password\":\"$PERF_ADMIN_PASSWORD\"}")
+          note "login probe -> HTTP $login_status"
+          if [ "$login_status" != "200" ] && [ "$login_status" != "201" ]; then
+            note "  $(head -c 400 /tmp/login.json 2>/dev/null)"
+            note "::error::login probe for $PERF_ADMIN_EMAIL returned HTTP $login_status"
+            exit 1
+          fi
+
+      - name: Seed load-test tickets (detail, messages and the dashboard need them)
         env:
           PERF_ADMIN_EMAIL: perf-admin@example.test
           PERF_ADMIN_PASSWORD: Perf-Smoke-1!
+          SEED_TICKETS: "120"
         run: |
-          # The install wizard is the supported way to bootstrap the first admin and
-          # the starting catalogue; the second run in the same database is a no-op.
-          status=$(curl -sS -o /tmp/super-admin.json -w '%{http_code}' \
-            -X POST "$BASE_URL/install/super-admin" \
+          # The install wizard seeds the catalogue but no tickets, so until this step the
+          # agent flow had nothing to open: `GET /tickets/:id`, `POST …/messages` and the
+          # dashboard summary reported p95 = 0 — three budgets "passed" without measuring
+          # anything. Creating 120 tickets through the public API also exercises the same
+          # code path the smoke then measures.
+          #
+          # The two ids come from the seed the wizard just created: `/install/seed` makes
+          # the organisational unit, an ACTIVE service and the routing rule (OU + service
+          # -> fallback group), so a ticket created with them is `PENDING` with a group —
+          # which is exactly what the group inbox lists. Created without the routing rule
+          # the ticket would be `UNROUTED`, invisible in the inbox, and the detail and
+          # message endpoints would still report 0,00.
+          set -euo pipefail
+          read_json() { python3 -c "import json,sys; d=json.load(sys.stdin); print($1)"; }
+          rm -f /tmp/seed-tickets.log
+          note() { echo "$*" | tee -a /tmp/seed-tickets.log; }
+
+          token=$(curl -sS -X POST "$BASE_URL/auth/login" \
             -H 'content-type: application/json' \
-            -d "{\"email\":\"$PERF_ADMIN_EMAIL\",\"displayName\":\"Perf Smoke\",\"password\":\"$PERF_ADMIN_PASSWORD\"}")
-          echo "super-admin: HTTP $status"
-          curl -sS -X POST "$BASE_URL/install/seed" > /tmp/seed.json
-          echo "seed: $(head -c 200 /tmp/seed.json)"
+            -d "{\"email\":\"$PERF_ADMIN_EMAIL\",\"password\":\"$PERF_ADMIN_PASSWORD\"}" \
+            | read_json 'd.get("accessToken","")')
+          if [ -z "$token" ]; then
+            echo "::error::no access token — cannot seed tickets"
+            exit 1
+          fi
+          auth="Authorization: Bearer $token"
+
+          seed_file=/tmp/install-install-seed.json
+          organizational_unit_id=""
+          service_id=""
+          if [ -f "$seed_file" ]; then
+            organizational_unit_id=$(read_json 'd["organizationalUnit"]["id"]' < "$seed_file" || true)
+            service_id=$(read_json 'd["service"]["id"]' < "$seed_file" || true)
+          fi
+          if [ -z "$organizational_unit_id" ] || [ -z "$service_id" ]; then
+            note "seed response carried no ids; reading them from the public API instead"
+            service_id=$(curl -sS -H "$auth" "$BASE_URL/services?lifecycle=ACTIVE" \
+              | read_json '(d if isinstance(d, list) else d.get("items", []))[0]["id"]')
+            organizational_unit_id=$(curl -sS -H "$auth" "$BASE_URL/organizational-units/tree" \
+              | python3 -c "import json,sys
+          def walk(nodes):
+              for node in nodes or []:
+                  if isinstance(node, dict) and node.get('id'):
+                      return node['id']
+                  if isinstance(node, dict):
+                      found = walk(node.get('children'))
+                      if found:
+                          return found
+              return ''
+          print(walk(json.load(sys.stdin)))")
+          fi
+          if [ -z "$service_id" ] || [ -z "$organizational_unit_id" ]; then
+            echo "::error::no service (or no organisational unit) to create tickets in — is the install seed in place?"
+            exit 1
+          fi
+          note "seeding $SEED_TICKETS tickets: service $service_id, origin unit $organizational_unit_id"
+
+          created=0
+          failed=0
+          for index in $(seq 1 "$SEED_TICKETS"); do
+            status=$(curl -sS -o /tmp/ticket.json -w '%{http_code}' \
+              -X POST "$BASE_URL/tickets" -H "$auth" -H 'content-type: application/json' \
+              -d "{\"title\":\"Perf smoke ticket $index\",\"description\":\"Seeded by the perf-smoke workflow so the detail, message and dashboard endpoints are measurable. Ticket $index of $SEED_TICKETS; safe to delete.\",\"impact\":\"MEDIUM\",\"urgency\":\"MEDIUM\",\"serviceId\":\"$service_id\",\"originUnitId\":\"$organizational_unit_id\",\"acknowledgeDuplicate\":true}")
+            case "$status" in
+              2*) created=$((created + 1)) ;;
+              *) failed=$((failed + 1)); [ "$failed" -le 3 ] && head -c 200 /tmp/ticket.json ;;
+            esac
+          done
+          note "tickets created: $created, failed: $failed"
+
+          if [ "$created" -eq 0 ]; then
+            echo "::error::no ticket could be created — the smoke would measure an empty app"
+            exit 1
+          fi
+
+          inbox=$(curl -sS -H "$auth" "$BASE_URL/tickets/inbox?pageSize=5" \
+            | read_json 'len(d.get("items", []) if isinstance(d, dict) else d)')
+          note "tickets visible in /tickets/inbox: $inbox"
+          if [ "$inbox" -eq 0 ]; then
+            # Not fatal, but it is the difference between "the agent flow measured nothing"
+            # and a real number: the group inbox lists unassigned PENDING tickets with a
+            # group, so an empty inbox means routing did not assign one.
+            note "::warning::the group inbox is empty — \`GET /tickets/:id\` and the message endpoint will report 0,00"
+          fi
 
       - uses: grafana/setup-k6-action@v1
 
       - name: Run the k6 smoke with CI thresholds
-        working-directory: .
+        # 200 VU / 5 min profile; the ceiling is here so a stalled run fails the job
+        # instead of sitting in the queue for hours (the first honest run did exactly that).
+        timeout-minutes: 20
         env:
           REPORT_LABEL: ci-smoke
           AGENT_EMAIL: perf-admin@example.test
@@ -127,21 +276,99 @@ jobs:
           REQUESTER_EMAIL: perf-admin@example.test
           REQUESTER_PASSWORD: Perf-Smoke-1!
         run: |
-          k6 run perf/smoke.js | tee perf-smoke.log
+          # `set -o pipefail` matters: otherwise the pipeline reports `tee`'s status (0),
+          # so a broken threshold or an aborted run left the job green — which is why the
+          # 200 VU "gate" could not block anything.
+          set -o pipefail
+          k6 run perf/smoke.js 2>&1 | tee perf-smoke.log
           echo "--- threshold verdict ---"
-          grep -E '^(✓|✗|•)' perf-smoke.log || true
+          grep -E '(✓|✗|•)' perf-smoke.log || true
 
       - name: Check the query budget from the API log
+        id: query-budget
+        # Also runs when the k6 step failed: the log still carries the numbers, and those
+        # numbers are what explains the failure.
+        if: always()
         run: |
-          # Custom threshold (plan §4.2): average `db_queries_per_request` across all
-          # finished requests must stay at or below the documented budget of 3.
-          read -r average count <<<"$(awk -F'db_queries_per_request=' '
-            /db_queries_per_request=/ { split($2, parts, " "); sum += parts[1]; n += 1 }
-            END { if (n == 0) { print "0 0" } else { printf "%.3f %d", sum / n, n } }' api.log)"
-          echo "requests logged: $count"
-          echo "avg db_queries_per_request: $average"
-          if [ "$count" -gt 0 ] && awk -v avg="$average" 'BEGIN { exit !(avg > 3) }'; then
-            echo "::error::avg db_queries_per_request=${average} exceeds the budget of 3"
+          # Custom threshold (plan §4.2): the average `db_queries_per_request` must stay at
+          # or below the documented budget (DB_QUERY_BUDGET, default 3).
+          #
+          # Bootstrap traffic (`/install/*`, `/health`) is reported separately and NOT
+          # counted into the average: it runs once per stack (super-admin + seed) and is not
+          # part of the load profile the budget describes. Everything else counts.
+          rm -f /tmp/app-queries.txt /tmp/query-budget-paths.txt
+          awk '
+            # Nest colorizes its log lines. Stripping the escape sequences in a rule that
+            # runs before the first field access matters: awk splits the record into fields
+            # lazily, so a later `gsub` leaves "/tickets\x1b[39m" beside "/tickets" and
+            # halves every per-route count. The CSI remnant ("[39m") is removed first, the
+            # ESC byte second.
+            { gsub(/\[[0-9;]*m/, ""); gsub(/[^[:print:]]/, "") }
+            /db_queries_per_request=/ {
+              query = ""; path = "";
+              for (i = 1; i <= NF; i++) {
+                if ($i ~ /^db_queries_per_request=/) { split($i, pair, "="); query = pair[2] + 0 }
+                if ($i ~ /^path=/) { split($i, pair, "="); path = pair[2] }
+              }
+              if (path == "") path = "?";
+              total_sum += query; total_n += 1;
+              # Bracket expression instead of `\/`: awk implementations disagree about
+              # an escaped slash inside a regex, and the first CI runs classified the
+              # bootstrap requests as application traffic because of it.
+              if (path ~ /^[/](install|health)/ || path ~ /^[/]auth[/](login|logout|refresh)/) {
+                bootstrap_sum += query; bootstrap_n += 1;
+              } else {
+                app_sum += query; app_n += 1;
+                print query > "/tmp/app-queries.txt";
+                path_sum[path] += query; path_n[path] += 1;
+              }
+            }
+            END {
+              printf "TOTAL_N=%d\nTOTAL_AVG=%.3f\nAPP_N=%d\nAPP_AVG=%.3f\nBOOTSTRAP_N=%d\n",
+                total_n, (total_n ? total_sum / total_n : 0), app_n,
+                (app_n ? app_sum / app_n : 0), bootstrap_n;
+              for (p in path_sum)
+                printf "%.3f %d %s\n", path_sum[p] / path_n[p], path_n[p], p > "/tmp/query-budget-paths.txt";
+            }
+          ' api.log | tee /tmp/query-budget.env
+
+          . /tmp/query-budget.env
+          # Gate = the measured baseline plus headroom (the plan's iron rule: a threshold
+          # comes from a measurement, not from an aspiration). TARGET is the engineering
+          # goal tracked in PERF_BUDGETS.md; the gap is a work item, not a hidden failure.
+          BUDGET="${DB_QUERY_BUDGET:-8}"
+          TARGET="${DB_QUERY_TARGET:-5}"
+          P95=0
+          MAX=0
+          if [ -s /tmp/app-queries.txt ]; then
+            P95=$(sort -n /tmp/app-queries.txt | awk '{ a[NR] = $1 } END { i = int(NR * 0.95); if (i < 1) i = 1; print a[i] }')
+            MAX=$(sort -n /tmp/app-queries.txt | tail -1)
+          fi
+          echo "avg (sve):              ${TOTAL_AVG} preko ${TOTAL_N} zahtjeva"
+          echo "avg (aplikacija):       ${APP_AVG} preko ${APP_N} zahtjeva   <- kapija ${BUDGET} (baseline + headroom), cilj ${TARGET}"
+          echo "p95 / max (aplikacija): ${P95} / ${MAX}"
+          echo "bootstrap (izuzeto):    ${BOOTSTRAP_N} zahtjeva (install/health/auth-login)"
+          echo "najskuplje rute:"
+          sort -k1,1nr /tmp/query-budget-paths.txt 2>/dev/null | head -5 | sed 's/^/  /' || true
+
+          {
+            echo "| Metrika | Vrijednost |"
+            echo "|---|---|"
+            echo "| avg \`db_queries_per_request\` (aplikacija) | **${APP_AVG}** preko ${APP_N} zahtjeva (kapija ${BUDGET}, cilj ${TARGET}) |"
+            echo "| p95 / max (aplikacija) | ${P95} / ${MAX} |"
+            echo "| avg (sve, s bootstrapom) | ${TOTAL_AVG} preko ${TOTAL_N} |"
+            echo "| bootstrap (\`/install/*\`, \`/health\`, \`/auth/login|logout|refresh\` — izuzeto iz kapije) | ${BOOTSTRAP_N} |"
+            echo ""
+            echo "<details><summary>najskuplje rute po prosjeku upita (uključuje i izuzete rute)</summary>"
+            echo ""
+            echo '```'
+            sort -k1,1nr /tmp/query-budget-paths.txt 2>/dev/null | head -8 || true
+            echo '```'
+            echo "</details>"
+          } > /tmp/query-budget-report.md
+
+          if [ "$APP_N" -gt 0 ] && awk -v avg="$APP_AVG" -v budget="$BUDGET" 'BEGIN { exit !(avg > budget) }'; then
+            echo "::error::avg db_queries_per_request=${APP_AVG} (aplikacija) prelazi budžet od ${BUDGET}"
             exit 1
           fi
 
@@ -156,3 +383,75 @@ jobs:
             perf-smoke.log
             api.log
           retention-days: 30
+
+      - name: Publish the numbers on the pull request
+        if: always() && github.event_name == 'pull_request'
+        env:
+          GH_TOKEN: ${{ github.token }}
+          PR_NUMBER: ${{ github.event.pull_request.number }}
+          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
+        run: |
+          # The verdict is read from the rendered report rather than the k6 console block:
+          # its layout varies between k6 versions and produced a constant "0 / 0".
+          # `|| true`, not `|| echo 0`: `grep -c` already prints 0 and exits 1, so the
+          # fallback used to print a second line ("9 prošlo, 0\n0 palo").
+          thresholds_ok=$(grep -cE '\| ok \|' perf/results/ci-smoke.md 2>/dev/null || true)
+          thresholds_failed=$(grep -cE 'FAILED' perf/results/ci-smoke.md 2>/dev/null || true)
+          {
+            echo "### Perf smoke — run [${{ github.run_id }}](${RUN_URL})"
+            echo ""
+            echo "**k6 pragovi:** ${thresholds_ok} prošlo, ${thresholds_failed} palo (CI pragovi = budžet × 2,5, \`perf/config.js\`)"
+            echo ""
+            echo "**Budžet upita**"
+            echo ""
+            cat /tmp/query-budget-report.md 2>/dev/null || echo "_korak budžeta nije stigao do izvještaja — vidi log runa_"
+            echo ""
+            if [ -f perf/results/ci-smoke.md ]; then
+              echo "<details><summary>k6 izvještaj (\`perf/results/ci-smoke.md\`)</summary>"
+              echo ""
+              echo '```'
+              head -90 perf/results/ci-smoke.md
+              echo '```'
+              echo "</details>"
+            fi
+            echo ""
+            echo "**Neuspjeli odgovori po ruti (iz api.log)**"
+            echo ""
+            echo '```'
+            sed -e 's/\x1b\[[0-9;]*[a-zA-Z]//g' api.log 2>/dev/null \
+              | grep -oE 'status=[0-9]+ method=[A-Z]+ path=[^ ]+' \
+              | grep -vE 'status=2[0-9][0-9]' | sort | uniq -c | sort -rn | head -10 \
+              || echo "(nijedan odgovor nije bio non-2xx)"
+            echo '```'
+            echo ""
+            echo "<details><summary>k6 izlaz (zadnjih 30 linija perf-smoke.log)</summary>"
+            echo ""
+            echo '```'
+            tail -30 perf-smoke.log 2>/dev/null || echo "(k6 nije ništa zapisao — run je stao prije prve iteracije)"
+            echo '```'
+            echo "</details>"
+            echo ""
+            echo "<details><summary>Seed tiketa — transkript</summary>"
+            echo ""
+            echo '```'
+            cat /tmp/seed-tickets.log 2>/dev/null || echo "(korak seeda nije pisao transkript)"
+            echo '```'
+            echo "</details>"
+            echo ""
+            echo "<details><summary>Install wizard — transkript</summary>"
+            echo ""
+            echo '```'
+            cat /tmp/install-wizard.log 2>/dev/null | tail -30 || echo "(korak wizarda nije pisao transkript)"
+            echo '```'
+            echo "</details>"
+            echo ""
+            echo "<details><summary>API log — login / seed / greške (zadnjih 25 linija)</summary>"
+            echo ""
+            echo '```'
+            grep -iE 'auth/login|install/seed|install/super-admin|error| 401| 403| 500' api.log 2>/dev/null | tail -25 || echo "(nema pogodaka)"
+            echo '```'
+            echo "</details>"
+            echo ""
+            echo "_Puni profil (2.800 VU / 30 min) se mjeri na stagingu — vidi \`ops/staging-checklist.md\`._"
+          } > /tmp/perf-comment.md
+          gh pr comment "$PR_NUMBER" --body-file /tmp/perf-comment.md
diff --git a/PERF_BUDGETS.md b/PERF_BUDGETS.md
index 061c476..3d88d77 100644
--- a/PERF_BUDGETS.md
+++ b/PERF_BUDGETS.md
@@ -15,10 +15,11 @@ regresija se ne spaja.
 |---|---|---|---|
 | P95 bilo kojeg read endpointa | < 200 ms | **nije mjereno** (kod-nalaz) | `perf/results/after-f3-2026-09-24.md` §5 — okruženje bez k6/Postgres/Redis/LB; struktura dokazana (paginacija, agregati, keš), mjeri se prvim `k6 run perf/full.js` |
 | P95 bilo koje mutacije | < 400 ms | **nije mjereno** (kod-nalaz) | isto |
-| DB upita po HTTP zahtjevu | ≤ 2 prosječno | instrumentirano, **nije mjereno** | `db_queries_per_request=` log (F0 middleware, `backend/src/common/request-context/request-metrics.middleware.ts`); authz keš: 0 upita po zahtjevu uz pogodak (after-f2 §2.2) |
+| DB upita po HTTP zahtjevu | cilj ≤ 5 prosječno (kapija 8) | **izmjereno (CI smoke 2026-09-24, 120 seedanih tiketa)**: 18,4 prije `perf-07` → 8,7 → **5,0** poslije keša labela (lista 12,1 · inbox 12,0 · detalj 14,0 · poruka 9,9 · `/search` 6,0 · dashboard 1,1 · unread 1,0) ⚠️ cilj otvoren | `db_queries_per_request=` iz `api.log` (F0 middleware, `backend/src/common/request-context/request-metrics.middleware.ts`); run [35985764662](https://github.com/mudzy992/help-desk-enterprise/actions/runs/35985764662), komentar na PR #2. **Kapija u CI-ju je 32** = izmjereno 21,2 × 1,5 (pravilo „prag iz mjerenja"); cilj ≤ 8 je radni zadatak, a ne tvrdnja. Nalaz: budžet „≤ 2" iz F1 odnosio se na *podatkovni* upit liste (paginacija + count), ne na cijeli zahtjev. Najveći dio ostatka bili su settingsi: 45 `*-configuration.loader.ts` fajlova čita svoje ključeve kroz `SettingsService`, jedan `await` (i jedan `findUnique`) po ključu — `perf-07` to svodi na jedno listanje tabele po zahtjevu (`settings-snapshot.ts`). Treći korak (isti dan) je keš kataloga: pet upita za *display labele* (korisnik, grupa, form version, jedinica, servis) čitalo se u svakom zahtjevu liste; sada se pamte po ID-u 60 s (`labels/ticket-label-cache.ts`, isti obrazac kao authz i dashboard keš, uključujući pamćenje „red ne postoji"). Ostalo su batch loaderi po stranici (`to-ticket-client-responses`, SLA snapshot, close codes, CSAT) i sesija/authz (keširano u Redisu). **Pouka iz mjerenja:** isti kod je na *praznoj* bazi davao 3,6 — `loadTicketDisplayLabels`, `loadParentTicketSummaries`, `loadTicketCsatSubmissions` i `loadCloseCodesByIds` preskaču upit kad nemaju šta razriješiti. Zato smoke od sada seeda 120 tiketa: prag koji se mjeri na praznoj aplikaciji ne mjeri ništa. Smanjenje: prvo izmjeriti koji loaderi se pozivaju na `status=RESOLVED` stranicama, pa spojiti u jedan upit / preskočiti kad polja nisu tražena |
 | DB QPS ukupno | < 1.000 uz pool 40 | nije mjereno | pool 40 / worker 10 (`DB_POOL_MAX`, F1.4); after-f1 §1 |
 | SLA scan ciklus | < 15 s @ 100k otvorenih | kod-nalaz: 2 statementa po ciklusu, `LIMIT 2000` | after-f2 §2.1 — due-only `nextDueAt` upit + batch `IN`; index `(resolutionCompletedAt, nextDueAt)` |
 | WS emit-ova / s | < 500 | kod-nalaz: ≈ 50/s (flag `off`) vs ≈ 2.040/s prije | after-f3 §2 — grupa od 200 ne dobija puni payload nego `group.feed-changed` < 200 B; metrika `ws_emits_*` je u logu |
+| WS emit-ova / s — **notifikacije (fan-out po korisniku)** | < 500 sve zajedno | **kod-nalaz, širi budžet**: grupa od 200 članova = 200 redova u `Notification` **i 200 emita** po događaju; pri 10 događaja/s ≈ 2.000/s — samostalno preko budžeta | `notifications/fan-out/publish-created-notifications.ts` (per-user emit) — `perf/results/after-f4-2026-09-24.md` §7 rizik 2; mjeri se `ops/sql/measure-notification-fan-out.sql` + `ws_emits_user` iz loga |
 | Dashboard payload | < 100 KB | kod-nalaz: agregatni summary, keš 15 s | after-f2 §2.4 — `GET /reports/dashboard/summary?scope=`, `GET /reports/sla/summary` |
 | Pretraga: zahtjeva po unosu | 1 | kod-nalaz: 1 zahtjev po unosu (AbortController + debounce) | after-f1 §2 + after-f3 §3 — `GET /search?q=…` |
 | Frontend zahtjevi po sesijskoj ruti | katalog ≤ 1 / staleTime | kod-nalaz: React Query keš (`queryKeys.*`, `staleTime` u `frontend/src/lib/query/query-client.ts`) umjesto ponovnog poziva po mountu | after-f3 §3 |
@@ -28,7 +29,7 @@ regresija se ne spaja.
 | k6 error rate (2.800 VU, 30 min) | < 0,5 % | < 1 % | budžet × 2 za dijeljene CI runnere i hladan JIT — komentar u `perf/config.js::ciThresholds` |
 | P95 read (CI) | < 200 ms | < 500 ms | budžet × 2,5: nema izmjerenog P95 iz F3 (kod-nalaz), pa se prag veže na budžet; iron rule „mjereno × 1,5" primjenjuje se na prvom CI runu s pravim brojkama |
 | P95 mutacija (CI) | < 400 ms | < 1.000 ms | isto, budžet × 2,5 |
-| avg `db_queries_per_request` (CI) | ≤ 2 | ≤ 3 | budžet + 1 (agregatni upiti računaju se kao jedan zahtjev); provjerava se iz `api.log` u CI koraku |
+| avg `db_queries_per_request` (CI) | ≤ 8 (cilj) | ≤ 32 (baseline × 1,5) | budžet + 1 (agregatni upiti računaju se kao jedan zahtjev); provjerava se iz `api.log` u CI koraku. **Bootstrap (`/install/*`, `/health`, `/auth/login|logout|refresh`) se izuzima iz kapije** — vrti se jednom po stacku odnosno po sesiji (`setup()`), pa nije dio profila opterećenja; ispisuje se odvojeno u istom koraku. Izmjereno: `POST /auth/login` ≈ 9 upita (2026-09-24) — nije u SLO tabeli, ali stoji u PR komentaru da se cijena vidi |
 
 ## 2. Kako se mjeri
 
diff --git a/perf/config.js b/perf/config.js
index 7617eb7..c6f27b4 100644
--- a/perf/config.js
+++ b/perf/config.js
@@ -52,6 +52,7 @@ export const perfConfig = {
     unreadCount: '/notifications/unread-count',
     notifications: '/notifications',
     search: '/search',
+    dashboardSummary: '/reports/dashboard/summary',
   },
   behaviour: {
     // One message every 30 s per virtual agent (plan §5.1).
@@ -114,7 +115,14 @@ export function ciThresholds(config) {
   const mutationP95Ms = config.budgets.mutationP95Ms * 2.5;
   const errorRate = config.budgets.errorRate * 2;
   return {
-    http_req_failed: [`rate<${errorRate}`],
+    // `abortOnFail`: once the error rate is past the gate there is nothing left to
+    // measure, so the run stops in seconds instead of spinning out its whole duration.
+    // Without it a broken `setup()` produced 10 million failed iterations in five
+    // minutes and a 35-minute CI job (the k6 call convention bug found on 2026-09-24;
+    // `perf/validate.js` now guards the arity statically).
+    http_req_failed: [
+      { threshold: `rate<${errorRate}`, abortOnFail: true, delayAbortEval: '10s' },
+    ],
     tickets_list_duration: [`p(95)<${readP95Ms}`],
     ticket_detail_duration: [`p(95)<${readP95Ms}`],
     unread_count_duration: [`p(95)<${readP95Ms}`],
@@ -129,7 +137,15 @@ export function ciThresholds(config) {
 /** Threshold block for the full run; smoke.js overrides the durations. */
 export function fullThresholds(config) {
   return {
-    http_req_failed: [`rate<${config.budgets.errorRate}`],
+    // Same reasoning as in `ciThresholds`, with a longer grace period: a 30-minute
+    // baseline that is 100% errors is not a baseline.
+    http_req_failed: [
+      {
+        threshold: `rate<${config.budgets.errorRate}`,
+        abortOnFail: true,
+        delayAbortEval: '30s',
+      },
+    ],
     tickets_list_duration: [`p(95)<${config.budgets.readP95Ms}`],
     ticket_detail_duration: [`p(95)<${config.budgets.readP95Ms}`],
     unread_count_duration: [`p(95)<${config.budgets.readP95Ms}`],
diff --git a/perf/lib/scenarios.js b/perf/lib/scenarios.js
index 7ea9d34..23e9d5d 100644
--- a/perf/lib/scenarios.js
+++ b/perf/lib/scenarios.js
@@ -42,6 +42,16 @@ export function browserDashboard(config, data) {
   expectOk(unread, 'notifications.unreadCount');
   recordResponse('notifications.unreadCount', unread, config);
 
+  // A Phase 2.4 endpoint that `PERF_BUDGETS.md` gives both a P95 and a payload budget.
+  // Nothing called it, so `dashboard_summary_duration` and `dashboard_payload_kb` were
+  // "ok" with p95 = 0 — a threshold that cannot fail is not a gate.
+  const summary = http.get(`${config.baseUrl}${config.paths.dashboardSummary}`, {
+    ...headers,
+    tags: { endpoint: 'reports.dashboardSummary' },
+  });
+  expectOk(summary, 'reports.dashboardSummary');
+  recordResponse('reports.dashboardSummary', summary, config);
+
   sleep(thinkTime(config));
 }
 
@@ -76,8 +86,12 @@ export function agentTicketFlow(config, data) {
     const message = http.post(
       `${config.baseUrl}${config.paths.tickets}/${ticketId}/messages`,
       JSON.stringify({
+        // `CreateTicketMessageDto` is exactly `{ type, body }`, and the controller runs
+        // with `forbidNonWhitelisted: true`: the earlier `visibility` field would have
+        // been rejected with a 400 the moment this branch became reachable (the inbox
+        // used to be empty, so the endpoint was never actually called).
+        type: 'AGENT_REPLY',
         body: `load test probe ${now}`,
-        visibility: 'staff',
       }),
       { ...headers, tags: { endpoint: 'tickets.message' } },
     );
@@ -99,11 +113,14 @@ export function searchHeavy(config, data) {
   const headers = authorizedHeaders(pickToken(data.tokens, __VU));
   const probe = `${config.behaviour.searchTerm}-${__VU}`;
 
-  const tickets = http.get(
-    `${config.baseUrl}${config.paths.tickets}?search=${encodeURIComponent(probe)}`,
-    { ...headers, tags: { endpoint: 'search.legacyTickets' } },
-  );
-  expectOk(tickets, 'search.legacyTickets');
+    // `q`, not `search`: Phase 1.1 renamed the list parameter, and the old name now gets
+    // a 400 from the validation pipe — which the smoke read as "the app is failing" while
+    // it was the harness using a stale contract (20 failed requests per run).
+    const tickets = http.get(
+      `${config.baseUrl}${config.paths.tickets}?q=${encodeURIComponent(probe)}`,
+      { ...headers, tags: { endpoint: 'search.listQuery' } },
+    );
+    expectOk(tickets, 'search.listQuery');
 
   const search = http.get(
     `${config.baseUrl}${config.paths.search}?q=${encodeURIComponent(probe)}&types=ticket,article,user`,
