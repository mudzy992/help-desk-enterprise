# CHECKLIST — F9-1

- [x] `killSwitchEnabled` polaritet potvrđen (A)
- [x] Settings + permissions + MATRIX
- [x] `eventId` + `createdAt` na `notification.created`
- [x] EDGE_EVENT enqueue iz fan-out
- [x] `GET /edge-extension/bootstrap` + `POST /edge-extension/receipts` + unit testovi
- [x] CORS CSV (bez `*`)
- [x] `edge-extension/` MV3: WS, poll fallback, redacted toast, dedup, receipts, popup shell
- [ ] Manual: load unpacked — connect, toast bez sadržaja, receipt, duplikat, WS drop → poll (nije moguće iz IDE browsera)
- [x] Označeno `[x]` u TASKS.md po zahtjevu korisnika
