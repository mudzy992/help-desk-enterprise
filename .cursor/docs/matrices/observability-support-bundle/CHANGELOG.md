# CHANGELOG — observability-support-bundle

## 2026-09-14
- Inicijalna matrica: HTTP/Socket `requestId` kroz ALS + logger + ExceptionFilter (`{ code, message, details, requestId }`), SuperAdmin `GET /support-bundle` zip prema settings flagovima, in-memory request log ring (API proces), reuse F8-1 snapshot i F8-2 audit serialize.
