import { PlaceholderPage } from "@/pages/placeholder-page";

export function RoutingPage() {
  return (
    <PlaceholderPage
      heading="No routing rules yet"
      body="Routing rules and administration will appear here when the routing module is implemented."
    />
  );
}
